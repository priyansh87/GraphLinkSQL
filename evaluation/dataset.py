import json
from typing import List, Dict

# A robust golden dataset for evaluation (50 questions)
GOLDEN_DATASET = [
    # --- Deep GraphRAG Multi-Hop JOINs ---
    {
        "question": "List customers who purchased products from the Bikes category.",
        "ground_truth_sql": "SELECT DISTINCT c.FirstName, c.LastName FROM Customer c JOIN SalesOrderHeader soh ON c.CustomerID = soh.CustomerID JOIN SalesOrderDetail sod ON soh.SalesOrderID = sod.SalesOrderID JOIN Product p ON sod.ProductID = p.ProductID JOIN ProductCategory pc ON p.ProductCategoryID = pc.ProductCategoryID WHERE pc.Name = 'Bikes';"
    },
    {
        "question": "Find the total revenue generated by each product category.",
        "ground_truth_sql": "SELECT pc.Name, SUM(sod.LineTotal) FROM ProductCategory pc JOIN Product p ON pc.ProductCategoryID = p.ProductCategoryID JOIN SalesOrderDetail sod ON p.ProductID = sod.ProductID GROUP BY pc.Name;"
    },
    {
        "question": "Which products were purchased by customers living in Seattle?",
        "ground_truth_sql": "SELECT DISTINCT p.Name FROM Product p JOIN SalesOrderDetail sod ON p.ProductID = sod.ProductID JOIN SalesOrderHeader soh ON sod.SalesOrderID = soh.SalesOrderID JOIN Address a ON soh.ShipToAddressID = a.AddressID WHERE a.City = 'Seattle';"
    },
    {
        "question": "Find the customer who spent the most on products from the Clothing category.",
        "ground_truth_sql": "SELECT c.FirstName, c.LastName FROM Customer c JOIN SalesOrderHeader soh ON c.CustomerID = soh.CustomerID JOIN SalesOrderDetail sod ON soh.SalesOrderID = sod.SalesOrderID JOIN Product p ON sod.ProductID = p.ProductID JOIN ProductCategory pc ON p.ProductCategoryID = pc.ProductCategoryID WHERE pc.Name = 'Clothing' GROUP BY c.CustomerID ORDER BY SUM(sod.LineTotal) DESC LIMIT 1;"
    },
    {
        "question": "List all product descriptions for products purchased by Orlando Gee.",
        "ground_truth_sql": "SELECT DISTINCT pd.Description FROM ProductDescription pd JOIN ProductModelProductDescription pmpd ON pd.ProductDescriptionID = pmpd.ProductDescriptionID JOIN Product p ON pmpd.ProductModelID = p.ProductModelID JOIN SalesOrderDetail sod ON p.ProductID = sod.ProductID JOIN SalesOrderHeader soh ON sod.SalesOrderID = soh.SalesOrderID JOIN Customer c ON soh.CustomerID = c.CustomerID WHERE c.FirstName = 'Orlando' AND c.LastName = 'Gee';"
    },
    {
        "question": "Find the product category generating the highest revenue among customers in London.",
        "ground_truth_sql": "SELECT pc.Name FROM ProductCategory pc JOIN Product p ON pc.ProductCategoryID = p.ProductCategoryID JOIN SalesOrderDetail sod ON p.ProductID = sod.ProductID JOIN SalesOrderHeader soh ON sod.SalesOrderID = soh.SalesOrderID JOIN Address a ON soh.ShipToAddressID = a.AddressID WHERE a.City = 'London' GROUP BY pc.ProductCategoryID ORDER BY SUM(sod.LineTotal) DESC LIMIT 1;"
    },
    {
        "question": "Which customers purchased products that have a catalog description?",
        "ground_truth_sql": "SELECT DISTINCT c.FirstName, c.LastName FROM Customer c JOIN SalesOrderHeader soh ON c.CustomerID = soh.CustomerID JOIN SalesOrderDetail sod ON soh.SalesOrderID = sod.SalesOrderID JOIN Product p ON sod.ProductID = p.ProductID JOIN ProductModel pm ON p.ProductModelID = pm.ProductModelID WHERE pm.CatalogDescription IS NOT NULL;"
    },
    {
        "question": "Find all product models associated with products purchased by customers in Seattle.",
        "ground_truth_sql": "SELECT DISTINCT pm.Name FROM ProductModel pm JOIN Product p ON pm.ProductModelID = p.ProductModelID JOIN SalesOrderDetail sod ON p.ProductID = sod.ProductID JOIN SalesOrderHeader soh ON sod.SalesOrderID = soh.SalesOrderID JOIN Address a ON soh.ShipToAddressID = a.AddressID WHERE a.City = 'Seattle';"
    },
    {
        "question": "List the categories purchased by customers whose orders were shipped to London.",
        "ground_truth_sql": "SELECT DISTINCT pc.Name FROM ProductCategory pc JOIN Product p ON pc.ProductCategoryID = p.ProductCategoryID JOIN SalesOrderDetail sod ON p.ProductID = sod.ProductID JOIN SalesOrderHeader soh ON sod.SalesOrderID = soh.SalesOrderID JOIN Address a ON soh.ShipToAddressID = a.AddressID WHERE a.City = 'London';"
    },
    {
        "question": "Find the total quantity sold for each product category.",
        "ground_truth_sql": "SELECT pc.Name, SUM(sod.OrderQty) FROM ProductCategory pc JOIN Product p ON pc.ProductCategoryID = p.ProductCategoryID JOIN SalesOrderDetail sod ON p.ProductID = sod.ProductID GROUP BY pc.ProductCategoryID;"
    },

    # --- COUNT / Aggregations ---
    {
        "question": "What is the average list price of all products?",
        "ground_truth_sql": "SELECT AVG(ListPrice) FROM Product;"
    },
    {
        "question": "How many sales orders have been placed in total?",
        "ground_truth_sql": "SELECT COUNT(*) FROM SalesOrderHeader;"
    },
    {
        "question": "What is the maximum freight cost charged on any single order?",
        "ground_truth_sql": "SELECT MAX(Freight) FROM SalesOrderHeader;"
    },
    {
        "question": "How many distinct cities are there in the Address table?",
        "ground_truth_sql": "SELECT COUNT(DISTINCT City) FROM Address;"
    },
    {
        "question": "Find the total freight cost across all orders.",
        "ground_truth_sql": "SELECT SUM(Freight) FROM SalesOrderHeader;"
    },
    {
        "question": "Count how many customers have a company name provided.",
        "ground_truth_sql": "SELECT COUNT(*) FROM Customer WHERE CompanyName IS NOT NULL;"
    },
    {
        "question": "How many products do not have a color specified?",
        "ground_truth_sql": "SELECT COUNT(*) FROM Product WHERE Color IS NULL;"
    },
    {
        "question": "What is the total tax amount for all online orders?",
        "ground_truth_sql": "SELECT SUM(TaxAmt) FROM SalesOrderHeader WHERE OnlineOrderFlag = 1;"
    },
    {
        "question": "What is the average line total in the sales order details?",
        "ground_truth_sql": "SELECT AVG(LineTotal) FROM SalesOrderDetail;"
    },
    {
        "question": "How many products have a weight greater than 1000?",
        "ground_truth_sql": "SELECT COUNT(*) FROM Product WHERE Weight > 1000;"
    },
    {
        "question": "What is the total order quantity across all sales details?",
        "ground_truth_sql": "SELECT SUM(OrderQty) FROM SalesOrderDetail;"
    },
    {
        "question": "How many addresses are in the state/province of 'Washington'?",
        "ground_truth_sql": "SELECT COUNT(*) FROM Address WHERE StateProvince = 'Washington';"
    },

    # --- GROUP BY ---
    {
        "question": "Count the number of products in each product category.",
        "ground_truth_sql": "SELECT ProductCategoryID, COUNT(*) FROM Product GROUP BY ProductCategoryID;"
    },
    {
        "question": "Find the total amount due for each customer.",
        "ground_truth_sql": "SELECT CustomerID, SUM(TotalDue) FROM SalesOrderHeader GROUP BY CustomerID;"
    },
    {
        "question": "How many orders were placed by each customer?",
        "ground_truth_sql": "SELECT CustomerID, COUNT(*) FROM SalesOrderHeader GROUP BY CustomerID;"
    },
    {
        "question": "What is the average order quantity per product in the sales details?",
        "ground_truth_sql": "SELECT ProductID, AVG(OrderQty) FROM SalesOrderDetail GROUP BY ProductID;"
    },
    {
        "question": "Which city has the highest number of addresses?",
        "ground_truth_sql": "SELECT City FROM Address GROUP BY City ORDER BY COUNT(*) DESC LIMIT 1;"
    },
    {
        "question": "Count the number of sales order details for each sales order ID.",
        "ground_truth_sql": "SELECT SalesOrderID, COUNT(*) FROM SalesOrderDetail GROUP BY SalesOrderID;"
    },

    # --- Multi-table JOINs ---
    {
        "question": "Find the total amount of orders placed by customers in London.",
        "ground_truth_sql": "SELECT SUM(soh.TotalDue) FROM SalesOrderHeader soh JOIN CustomerAddress ca ON soh.CustomerID = ca.CustomerID JOIN Address a ON ca.AddressID = a.AddressID WHERE a.City = 'London';"
    },
    {
        "question": "Which customer has the highest total order value? Provide their first and last name.",
        "ground_truth_sql": "SELECT c.FirstName, c.LastName FROM Customer c JOIN SalesOrderHeader soh ON c.CustomerID = soh.CustomerID GROUP BY c.CustomerID ORDER BY SUM(soh.TotalDue) DESC LIMIT 1;"
    },
    {
        "question": "List the names of products ordered in order number '71774'.",
        "ground_truth_sql": "SELECT p.Name FROM Product p JOIN SalesOrderDetail sod ON p.ProductID = sod.ProductID JOIN SalesOrderHeader soh ON sod.SalesOrderID = soh.SalesOrderID WHERE soh.SalesOrderNumber = 'SO71774';"
    },
    {
        "question": "Find the first and last name of the customer who placed order number 'SO71774'.",
        "ground_truth_sql": "SELECT c.FirstName, c.LastName FROM Customer c JOIN SalesOrderHeader soh ON c.CustomerID = soh.CustomerID WHERE soh.SalesOrderNumber = 'SO71774';"
    },
    {
        "question": "List the names of products that belong to the 'Bikes' category.",
        "ground_truth_sql": "SELECT p.Name FROM Product p JOIN ProductCategory pc ON p.ProductCategoryID = pc.ProductCategoryID WHERE pc.Name = 'Bikes';"
    },
    {
        "question": "Find the total quantity ordered for the product named 'Mountain-100 Black, 38'.",
        "ground_truth_sql": "SELECT SUM(sod.OrderQty) FROM SalesOrderDetail sod JOIN Product p ON sod.ProductID = p.ProductID WHERE p.Name = 'Mountain-100 Black, 38';"
    },
    {
        "question": "List the order dates of all orders placed by the customer 'Orlando Gee'.",
        "ground_truth_sql": "SELECT soh.OrderDate FROM SalesOrderHeader soh JOIN Customer c ON soh.CustomerID = c.CustomerID WHERE c.FirstName = 'Orlando' AND c.LastName = 'Gee';"
    },
    {
        "question": "How many sales orders were shipped to an address in 'Seattle'?",
        "ground_truth_sql": "SELECT COUNT(*) FROM SalesOrderHeader soh JOIN Address a ON soh.ShipToAddressID = a.AddressID WHERE a.City = 'Seattle';"
    },
    {
        "question": "Find the total list price of all products in the 'Clothing' category.",
        "ground_truth_sql": "SELECT SUM(p.ListPrice) FROM Product p JOIN ProductCategory pc ON p.ProductCategoryID = pc.ProductCategoryID WHERE pc.Name = 'Clothing';"
    },
    {
        "question": "Find the customer who placed the most orders.",
        "ground_truth_sql": "SELECT c.FirstName, c.LastName FROM Customer c JOIN SalesOrderHeader soh ON c.CustomerID = soh.CustomerID GROUP BY c.CustomerID ORDER BY COUNT(*) DESC LIMIT 1;"
    },

    # --- Schema Reasoning / Business Questions ---
    {
        "question": "What are the names of products that have a description?",
        "ground_truth_sql": "SELECT DISTINCT p.Name FROM Product p JOIN ProductModelProductDescription pmpd ON p.ProductModelID = pmpd.ProductModelID;"
    },
    {
        "question": "Get the address line and city of the customer named 'Orlando Gee'.",
        "ground_truth_sql": "SELECT a.AddressLine1, a.City FROM Address a JOIN CustomerAddress ca ON a.AddressID = ca.AddressID JOIN Customer c ON ca.CustomerID = c.CustomerID WHERE c.FirstName = 'Orlando' AND c.LastName = 'Gee';"
    },
    {
        "question": "List the categories that have products costing more than $1000.",
        "ground_truth_sql": "SELECT DISTINCT pc.Name FROM ProductCategory pc JOIN Product p ON pc.ProductCategoryID = p.ProductCategoryID WHERE p.ListPrice > 1000;"
    },
    {
        "question": "What is the name of the product with the highest list price?",
        "ground_truth_sql": "SELECT Name FROM Product ORDER BY ListPrice DESC LIMIT 1;"
    },
    {
        "question": "Which sales order has the maximum total due?",
        "ground_truth_sql": "SELECT SalesOrderNumber FROM SalesOrderHeader ORDER BY TotalDue DESC LIMIT 1;"
    },
    {
        "question": "List all products that have a standard cost strictly greater than 500.",
        "ground_truth_sql": "SELECT Name FROM Product WHERE StandardCost > 500;"
    },
    {
        "question": "List the product categories that have no parent category.",
        "ground_truth_sql": "SELECT Name FROM ProductCategory WHERE ParentProductCategoryID IS NULL;"
    },
    {
        "question": "What is the total subtotal of orders placed in the year 2008?",
        "ground_truth_sql": "SELECT SUM(SubTotal) FROM SalesOrderHeader WHERE OrderDate LIKE '2008%';"
    },
    {
        "question": "List the product models that have a catalog description.",
        "ground_truth_sql": "SELECT Name FROM ProductModel WHERE CatalogDescription IS NOT NULL;"
    },
    {
        "question": "Which product has the lowest standard cost?",
        "ground_truth_sql": "SELECT Name FROM Product WHERE StandardCost IS NOT NULL ORDER BY StandardCost ASC LIMIT 1;"
    },
    {
        "question": "List the descriptions of product model ID 19.",
        "ground_truth_sql": "SELECT pd.Description FROM ProductDescription pd JOIN ProductModelProductDescription pmpd ON pd.ProductDescriptionID = pmpd.ProductDescriptionID WHERE pmpd.ProductModelID = 19;"
    },
    {
        "question": "What is the average freight cost for orders placed by customer ID 29825?",
        "ground_truth_sql": "SELECT AVG(Freight) FROM SalesOrderHeader WHERE CustomerID = 29825;"
    }
]

def load_dataset() -> List[Dict]:
    return GOLDEN_DATASET
